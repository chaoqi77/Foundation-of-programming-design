#include <iostream>
#include <sstream>
#include <string>
#include <string.h>

using namespace std;

string count_num(string s);
int main()
{
    char s[10] = {0};
    cin >> s;
    int len = strlen(s);
    //cout << strlen(len) ;
    return 0;
    char temp;
    // if (len > 0) {
    //     temp = s[len-1];
    //     if (temp == "y") {

    //     } else {
    //         cout << "乘机根为：" << s << endl;
    //     }
    // } else {
    //     cout << "请输入单词" << endl;
    // }
    // cout << s.length();

    return 0;
}
